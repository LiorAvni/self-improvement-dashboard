// Simple client-side gate for personal/private use only.
// Change these values before deploying if you want different local credentials.
// IMPORTANT: this is NOT real backend authentication; users can inspect client code.
export const DEFAULT_USERNAME = 'lior';
export const DEFAULT_PASSWORD = '0612';
export const AUTH_SESSION_KEY = 'six-month-dashboard:auth-session';
